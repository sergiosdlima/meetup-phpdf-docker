# Exemplo de aplicação php e banco de dados

```
$ docker-compose up -d
```

Neste exemplo:

  * docker-compose up cria todos os container especificados no arquivo docker-compose.yml
  * parâmetro -d roda os container em background
